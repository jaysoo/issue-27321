Example of using proxy with Webpack and Express.

```shell
nx serve api

# In a separate terminal
nx serve demo
```


Then go to http://localhost:4200, and click the button to fetch via proxy server.
